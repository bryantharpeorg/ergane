# 133-US4 verification outputs

Runtime evidence required by US4's acceptance scenarios (epic 133). Each file
holds pasted tool output; no prose substitutes for the run.

- `t048_corpus_parity_summary.txt` — T048: the parity test's summary line over
  the full corpus, naming the number of specs compared, with its pasted
  wall-clock time (FR-009, plan step 7); and the red-first transcripts for the
  four guards, each against the wrong implementation it exists to guard
  against.