# Plan

The defective fixture's plan. It cites nothing and skips nothing; the refusal
and the advisory both come from `spec.md` and `tasks.md`.

Deliberately no coverage line for US1-S1: the missing task reference is the
advisory the capture freezes.