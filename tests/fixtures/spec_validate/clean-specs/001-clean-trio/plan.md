# Plan

The fixture plan. One page, one reader, no citations the shared target
repository can resolve.

The sentinel lives in `tasks.md`; the plan stays inert so the capture shows
exactly one information note.