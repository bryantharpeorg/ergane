# Fixture page with a near-miss command

This file exists so the near-miss detector can be proven against a deliberately
bad page. The committed `README.md` and `CLAUDE.md` are already clean, so a test
that only sweeps them would pass even if the detector were removed.

Run `nergane install` to configure the system.
