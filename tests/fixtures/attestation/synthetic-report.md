# Attestation: example:1/US1

## Completeness

- `test|coverage.txt|dispatch-1|capture-1`: `included` - retained declared artifact

Attachments are exact retained bytes. Opaque bytes are not redacted and may contain sensitive data.
