# Plan: Orphan tasks and a quoted example

One store, one code minter, one redirect. Nothing here is interesting; the
fixture's whole content is in its `tasks.md`.

This file exists because prompt assembly reads the whole trio, and a fixture
missing one would exercise the missing-file path by accident.
