# US8 verification outputs

Runtime evidence required by US8's acceptance scenarios (epic 133). Each file
holds pasted tool output; no prose substitutes for the run.

- `t065_golden_comparison.txt` — T065: the comparison of `ergane spec validate`
  over both US1 fixture trios against the six committed golden artifacts, empty
  on all three streams, plus the assembled byte count against the 52,400-byte
  working ceiling and the byte-identity proof for the moved span.
