# Tasks: 025-ci-red-recovery

This task list is a skeleton. The implementer node works its slice test-first and commits once per task.

## Implementation

- [ ] T001 Implement the fix for `interpreter/ci-failure-never-reaches-an-agent` and run the gate command.
- [ ] T002 Implement the fix for `ci/flaky-concurrency-test-is-a-random-epic-killer` and run the gate command.

## Verification

- [ ] Final gate command passes green.
