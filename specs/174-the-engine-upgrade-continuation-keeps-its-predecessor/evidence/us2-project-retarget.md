# US2 the owned project persists and launches the requested image

All Docker/child calls are synthetic captures; only temporary real files are
used. No gate contacted Docker, a worker, a provider, a credential store or a
live Temporal service.

## T006 reds

```text
FAILED tests/test_engine_upgrade.py::test_compose_child_receives_requested_version_environment
    assert up_kwargs["env"]["ERGANE_VERSION"] == "0.4.0"
E       KeyError: 'env'
```

The real `_ComposeDockerSeam` built the version dict and then dropped it: the
actual `docker compose up -d --no-build` child received no environment at all.

```text
FAILED tests/test_engine_upgrade.py::test_upgrade_persists_cli_matched_image_and_version
    assert any("retargeted" in note for note in report.notes)
E       assert False
```

The literal old image stayed in the persisted `compose.yaml` and
`ERGANE_VERSION=0.3.0` stayed in the persisted `.env`, so `ERGANE_VERSION`
passed to Compose wrote no durable upgrade.

## T006/T010 green

```text
uv run pytest -q tests/test_engine_upgrade.py
35 passed in 0.14s
```

Persisted project (generated by `resolve_project`, persisted by
`write_project`): `compose.yaml` service image and `.env` `ERGANE_VERSION` both
read `0.4.0`, the manifest records both new digests, and the captured `up`
child carries `ERGANE_VERSION=0.4.0` in its subprocess environment with
`-f <project>/compose.yaml` and `cwd` at the project.

## T007 green

```text
uv run pytest -q tests/test_engine_upgrade.py::test_upgrade_preserves_unrelated_fields_and_updates_ownership_digests
1 passed
```

Exactly one compose line (`image:`) and one `.env` line (`ERGANE_VERSION=`)
changed; user `1111:2222`, port `127.0.0.1:9055:7233`, `apparmor=unconfined`,
the repo mount, `TEMPORAL_NAMESPACE=ergane` and every other byte survived;
`.env` gained no `KEY=/TOKEN=/SECRET=/PASSWORD=` assignment; ownership digests
were updated for the two retargeted files and carried for the two untouched
confinement artifacts.

## T008 green

```text
uv run pytest -q tests/test_engine_upgrade.py -k refuses_artifact
6 passed
```

`changed`, `unclaimed`, `missing`, `unsupported-compose`, `unsupported-env` and
`manifest-absent` each refuse naming the affected path before stop or write;
byte snapshots are identical and the seam log is empty, with and without
`--force` while an epic is in flight.

## T009 green

```text
uv run pytest -q tests/test_engine_upgrade.py -k persistence_failure
1 passed
```

A synthetic mid-transaction rename failure after validation raised
`could not commit the retargeted engine container project: <project>/.env
could not be replaced …; the previous project bytes were restored`; every
project byte matched the pre-run snapshot, `stop/start/verify/list_images/
remove_image` never ran, the manifest still owned the restored bytes, and a
subsequent invocation proceeded on the restored project.

## T011 green

```text
uv run pytest -q tests/test_engine_upgrade.py -k rereads_saved_selection
1 passed
```

A later invocation rewrites nothing (compose, `.env` and manifest
byte-identical), reports the project "already selects"
`ghcr.io/bryantharpeorg/ergane:0.4.0`, and both actual-boundary regressions
re-run green.

## T012 docs

`docs/cli/engine.md` and `docs/container.md` now distinguish the
captured-runner evidence from the still-required real drained-image/rollback
qualification; `--force` is documented as never bypassing ownership.

## T013 controls, hashes and bound

```text
uv run pytest -q tests/test_engine_upgrade.py tests/test_engine_identity.py
  tests/test_engine_identity_probe.py tests/test_engine_identity_repository.py
  tests/test_engine_identity_skew.py tests/test_engine_skew_preflight.py
  tests/test_engine_skew_status.py tests/test_container_project.py
  tests/test_container_manifest.py tests/test_readme.py
182 passed
```

```text
36bebd8446dc7c93578a28cb696fc8d62c9816162942d0e271d1d65782eb5b8e  README.md
bcc8df1afd2bcc3f7be4fcd6a5fd48bebc08764bf752ffb9f5d8dc87f205d5f1  docs/onramp-exercise.md
a7c3ef32d029a0acef23192c75a063f386b944e089bb51dfd5a107cff5944e52  docs/container.md
2b955c65dd873fe8bde3f8180315274868b92c89e83eb3cd9a643c9f42269498  docs/cli/engine.md
```

README and the on-ramp exercise are byte-identical to their dispatch baselines.

The story diff from its dispatch base (`57e1077`), this artifact included, is
51,894 bytes, within the 64 KiB bound.

Full declared gate:

```text
uv run pytest -q
6294 passed, 58 skipped in 660.99s (0:11:00)
```

The skipped tiers are environmental (live capacity, live epic, live merge,
live onramp, live proxy, live telegram) and require servers or credentials that
factory gates must not contact.
