# 090 US3 — attempt report

**Story**: the tick says which branch it used, who named it, and what head it
landed on.
**Node**: `us3`, branch `factory/090-the-factory-reads-origin-not-the-operators-checkout/us3`.

## What the record now carries

`_refresh_to_default` already held a `LandingBase` with three fields and kept
one and a half of them: the branch, the arm, and nothing of the detail. US3 adds
`CloneResult.default_detail` — the loader's own phrase, carried verbatim — and
`_log_clone_base`, the line an operator watching the worker reads while the line
is stopped.

The record and the line, produced by driving the real `_refresh_to_default`
against real clones with a real `origin` (tool output pasted, not described):

```
--- a clone whose manifest declares the branch ---
INFO factory.activities.roadmap_activities: roadmap clone base: dev (manifest: declared in ergane.yaml), read from /tmp/us3demo/declared; refreshed to 687d50661ec3622666fd5f8e7fd2827636d8999e
result: default_source='manifest' default_detail='declared in ergane.yaml'
--- a clone with no manifest ---
INFO factory.activities.roadmap_activities: roadmap clone base: main (checked-out-head: fell back to the checked-out HEAD: /tmp/us3demo/guessed/ergane.yaml: [missing_manifest] cannot be read (No such file or directory); every target repo must commit a ergane.yaml declaring its gates), read from /tmp/us3demo/guessed; refreshed to 6152cd26880d58c945fe4352c009be394789eb2a
result: default_source='checked-out-head' default_detail='fell back to the checked-out HEAD: /tmp/us3demo/guessed/ergane.yaml: [missing_manifest] cannot be read (No such file or directory); every target repo must commit a ergane.yaml declaring its gates'
--- a clone the refresh refuses (US2's path, US3's line) ---
INFO factory.activities.roadmap_activities: roadmap clone base: dev (manifest: declared in ergane.yaml), read from /tmp/us3demo/refused; refused to refresh, the clone still stands at 687d50661ec3622666fd5f8e7fd2827636d8999e
```

Two things that output shows and a field alone would not. The arm is on the line
even on the declared path — the reason `_log_landing_base` gives one activity
over, and it is exactly the second line above that pays for it: `main` there is a
guess, is shaped identically to a declaration, and only `checked-out-head` says
so. And the refusal path logs too: a refused refresh is something the tick *did*,
and a silent one reads like a tick that never ran.

## S3 was green on arrival, and that is the honest report

S1, S2 and the log test were red against the pre-US3 code — the refresh read
`LandingBase.detail` and dropped it, and emitted nothing.

S3 was not. US2's refusal already names the branch, the paths, the commit and the
cure, so the scenario asks for a *committed proof* of a property that landed with
US2 rather than for a change. What the new test adds is that the proof is over
the string an operator actually reads: US2's own workflow test parks a refusal
this test suite wrote, which leaves open whether the real `_work_at_risk` wording
survives the trip. So the refusal in
`tests/test_roadmap_scheduler.py::test_the_park_reason_an_operator_reads_names_branch_paths_and_the_cure`
is generated by the real refresh against a real clone carrying both kinds of work
at risk, then carried through the workflow's park path, and the assertions are
made against the parked finding's `detail`. No production code changed for it.

## T022 — the two functions FR-008 names are unchanged

Checked across the whole epic, from the commit before US1 to this branch's head,
not just across this story:

```
$ git diff --stat 90033f6..HEAD
 factory/activities/roadmap_activities.py           | 219 ++++++++++++++-
 factory/roadmap/workflow.py                        |  14 +-
 .../attempt-report-us2.md                          | 170 ++++++++++++
 tests/test_090_refresh_follows_manifest.py         | 299 +++++++++++++++++++++
 tests/test_090_refusal_parks_the_spec.py           | 290 ++++++++++++++++++++
 tests/test_090_the_tick_names_its_branch.py        | 197 ++++++++++++++
 tests/test_roadmap_scheduler.py                    | 137 +++++++++-
 7 files changed, 1314 insertions(+), 12 deletions(-)
```

The module those two functions live in is absent from that list — the epic
changed one activity module, one workflow, and tests. A targeted diff of it
across the same range returns nothing at all:

```
$ git diff 90033f6..HEAD -- <the module FR-008 names> | wc -l
0
```

Which is the outcome trap 4 asks for: the resolver still fails open, US3 reports
that it did rather than repairing it, and 107's decision is inherited rather than
re-litigated.

## Gate

```
$ uv run pytest -q
5203 passed, 58 skipped, 8 warnings in 365.69s (0:06:05)
```

## What remains the operator's

The plan's § *Verification the operator will run* — a live roadmap tick against a
real clone with work on it — is unchanged by this story and still outstanding for
the same reason US2's report gives: the live worker imports the landing branch's
code, and this epic is not on it yet. When it is, the line this story adds is
what makes that verification readable from the worker log rather than from a
reflog after the fact.
