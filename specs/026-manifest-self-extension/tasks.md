# Tasks: 026-manifest-self-extension

This task list is a skeleton. The implementer node works its slice test-first and commits once per task.

## Implementation

- [ ] T001 Implement the fix for `interpreter/manifest-schema-cannot-be-extended-by-a-story` and run the gate command.

## Verification

- [ ] Final gate command passes green.
