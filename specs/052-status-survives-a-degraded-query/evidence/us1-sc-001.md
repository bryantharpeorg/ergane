# 052-US1 evidence — SC-001, against the live control plane

SC-001 is a claim about a running deployment, so it cannot be proved by a test.
This file is the transcript, pasted rather than described (constitution VIII /
D-037: the judge sees this diff and the criteria, never a terminal).

Measured 2026-08-17 03:59Z (2026-08-16 10:59 PM CT) against
`localhost:7233`, namespace `factory` — this repository's own control plane,
whose newest `roadmap-specs*` run refuses `roadmap_status` today.

**Exit codes are captured with `rc=$?` immediately after the command and before
any pipe.** `cmd | head; echo $?` reports the *pipe's* status, and SC-001 is an
exit code, so a piped measurement would have measured the wrong process
entirely. Both runs below redirect to a file and read it afterwards.

## The floor this was measured against

The roadmap runs on this namespace are `Failed`, and the newest were created by
a leaked test schedule (`ergane-roadmap-declared`) pointing at a
`/tmp/pytest-of-admin/.../checkout-dir/specs` corpus. `resolve_roadmap` picks the
newest `roadmap-specs*` run whether or not it failed, so both `ergane status`
and `ergane roadmap status` query a dead run:

    $ # read-only listing, newest first
    roadmap-specs-2026-08-16T17:45:00Z FAILED    2026-08-16 17:45:00.225075+00:00
    roadmap-specs-2026-08-16T17:40:00Z FAILED    2026-08-16 17:40:00.102777+00:00
    roadmap-specs-2026-08-16T17:35:00Z FAILED    2026-08-16 17:35:00.069452+00:00
    roadmap-specs-2026-08-16T17:30:00Z FAILED    2026-08-16 17:30:00.209598+00:00
    roadmap-specs-2026-08-15T09:40:00Z COMPLETED 2026-08-15 14:41:14.857592+00:00
    roadmap runs seen: 93

That schedule is out of scope here and was not touched. Nothing in this story
starts, terminates, signals or migrates anything on this namespace.

## Before — the guard as `46c50f5` wrote it

Reproduced in this worktree by reverting both `RPCError`-only clauses to their
`46c50f5` shape (`_disposition`'s query guard and the band in `collect_floor`),
purging `__pycache__`, and running against the same live control plane:

    $ timeout 120 uv run ergane status specs > us1-before.out 2> us1-before.err; rc=$?
    EXIT CODE (captured before any pipe) = 1
    --- stdout ---
    --- stderr ---
    ergane: unexpected error (RoadmapStatus.__init__() missing 1 required positional argument: 'max_concurrent_nodes'); re-run with --debug for the traceback

Nothing on stdout. The corpus half of the report — the queue, the drafts, the
readiness basis, none of which needs Temporal at all — is lost to a degradation
the command was built to survive.

## After — this diff

    $ timeout 120 uv run ergane status specs > us1-after.out 2> us1-after.err; rc=$?
    EXIT CODE (captured before any pipe) = 0
    --- stdout ---
    roadmap
      schedule: ergane-roadmap (paused)
      run: roadmap-specs-2026-08-16T17:45:00Z
      next tick: 2026-08-15T20:00:00+00:00
      dispatch: unavailable (RoadmapStatus.__init__() missing 1 required positional argument: 'max_concurrent_nodes')

    epics
      epic 024-ledger-token-honesty  RUNNING  execution RUNNING
      us1  RUNNING  attempt 1  factory/024-ledger-token-honesty/us1

    queue (readiness: attestation, plus landings on ergane-buildout (4426faa48330) in <worktree>, read without fetching)
      022-validate-calibration              ready  dispatchable
      023-composable-verification           ready  dispatchable
      024-ledger-token-honesty              ready  dispatchable
      035-operator-completion               ready  dispatchable
      042-supervised-services               ready  dispatchable
      050-init-preconditions                ready  dispatchable
      051-first-run-defaults                ready  dispatchable
      052-status-survives-a-degraded-query  ready  dispatchable

    drafts
      017-peer-channel        draft
      032-replay-determinism  draft

    pace
      024-ledger-token-honesty
        no attempt recorded for this epic yet
        1 of 1 stories remaining
      measured wall-times of finished attempts only
    --- stderr ---

Exit 0, stderr empty, and the refusal is reported *in the roadmap section* with
its own message rather than as a note replacing the report (FR-002). The three
lines above it — schedule, run, next tick — came from the discovery ladder, not
from the query, so they are still true and still printed.

The epics section is the reason the verb matters: `024-ledger-token-honesty` is
running right now, on the same screen the refused query used to take down.

## The backstop, measured rather than asserted

Reverting **only** `_disposition`'s query guard, leaving the band in
`collect_floor` repaired, does not reproduce the defect:

    EXIT CODE (captured before any pipe) = 0
    note: a workflow refused a query (RoadmapStatus.__init__() missing 1 required positional argument: 'max_concurrent_nodes'); the reading it would have provided is missing from this report

    roadmap
      unavailable (see note)
    epics
      unavailable (see note)
    ...
    pace
      unavailable (see note)

A worse report — the disposition and the running epic are both lost, because
the band cannot know which reading failed — but still exit 0 and still the whole
corpus half. That is the `QUERY_REFUSED` clause in `collect_floor` doing exactly
the job its comment claims: a query whose own call site forgot to guard it
degrades the report instead of killing the command. It is a backstop, and this
is the measurement that shows it is reachable rather than decorative.

Reproducing `46c50f5` therefore needs *both* clauses reverted, which is the
"before" run above.
