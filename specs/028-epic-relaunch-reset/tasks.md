# Tasks: 028-epic-relaunch-reset

This task list is a skeleton. The implementer node works its slice test-first and commits once per task.

## Implementation

- [ ] T001 Implement the fix for `interpreter/relaunched-epic-resumes-the-dead-runs-tree` and run the gate command.

## Verification

- [ ] Final gate command passes green.
